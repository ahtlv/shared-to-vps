# Каталог запретов адаптера CMS. Подключается из default.conf сразу после
# запрета точечных файлов. Этот файл пустой намеренно: без него каталог не
# попал бы в отрендеренный стек, и монтирование создало бы его от root.
# Адаптер кладёт рядом свой файл, например wordpress.conf.
